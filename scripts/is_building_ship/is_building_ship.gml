function is_building_ship() {
	var building = argument[0]
	if building == "Scouter" || building == "Fighter" || building == "Cruiser"
	|| building == "Bomber" || building == "Harvester" || building == "Carrier"
		return true
	else
		return false


}
